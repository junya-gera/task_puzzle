<div class="top">
  <div class="top__container">
    <div class="login">
      <div class="login__top">
        <p>ログイン</p>
      </div>
      <div class="login__container">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="field">
            <%= f.label :メールアドレス %><br />
            <%= f.email_field :email, placeholder:"メールアドレス", autofocus: true, autocomplete: "email",class: "login__container--input" %>
          </div>

          <div class="field login__container--input">
            <%= f.label :パスワード %><br />
            <%= f.password_field :password, placeholder:"パスワード", autocomplete: "current-password",class: "login__container--input" %>
          </div>

          <% if devise_mapping.rememberable? %>
            <div class="field login__container--rememberable">
              <%= f.check_box :remember_me %>
              <%= f.label :ログインしたままにする %>
            </div>
          <% end %>

          <div class="actions">
            <%= f.submit "ログイン", class:"login__container--btn" %>
          </div>
          <div class="actions">
            <%= f.submit "かんたんログイン", class:"login__container--btn easybtn" %>
          </div>
        <% end %>

        <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
          <%= link_to "新規登録", new_registration_path(resource_name), class:"login__container--registration" %><br />
        <% end %>

        <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
          <%= link_to "パスワードをお忘れですか？", new_password_path(resource_name),class:"login__container--recover" %>
        <% end %>
      </div>
    </div>
    <div class="intro">
    </div>
  </div>
</div>